# dev 環境の設定値。環境ごとの差分はこのファイルに集約する(02-infra.md §2)。

env            = "dev"
retention_days = 1

# --- 確定したら埋める --------------------------------------------------------
# root_domain          = "example.com"        # OPS-01
# google_client_id     = "xxx.apps.googleusercontent.com"  # AUTH-07
# google_client_secret = "..."                # AUTH-07(tfvars には置かない。§README)
