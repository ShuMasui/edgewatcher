# shared スタックの設定値。

github_owner = "ShuMasui"
github_repo  = "edgewatcher"

# gh api users/ShuMasui --jq .id / gh api repos/ShuMasui/edgewatcher --jq .id
github_owner_id = "246949782"
github_repo_id  = "1340466250"

# --- 確定したら埋める --------------------------------------------------------
# root_domain = "example.com"   # OPS-01。値を入れると Route53 ホストゾーンが作られる
